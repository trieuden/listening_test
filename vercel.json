{
  "buildCommand": "npm run build",
  "outputDirectory": "build/client",
  "serverFiles": ["build/server/**"],
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/(.*)",
      "dest": "/api/index.js"
    }
  ]
}
